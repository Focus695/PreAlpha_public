# Remote API Configuration
# This API handles both data endpoints and SIWE authentication
# Required endpoints:
#   Data endpoints:
#     - GET  /leaderboard  → Leaderboard data
#     - GET  /signals      → Signal feed
#     - GET  /address/:id  → Address profile
#   Authentication endpoints (SIWE):
#     - GET  /auth/nonce   → Returns { nonce: string }
#     - POST /auth/verify  → Accepts { message, signature }, returns { ok, token }
#     - GET  /user/profile → Returns user data (requires JWT)
#     - POST /user/update  → Updates user data (requires JWT)
# See docs/FRONTEND_AUTH_IMPLEMENTATION.md for auth implementation details
API_BASE_URL=https://api.prealpha.trade

# WalletConnect Configuration
# Get your project ID from https://cloud.walletconnect.com/
WALLETCONNECT_PROJECT_ID=your_project_id_here

# Development Server
WEB_PORT=3000
NODE_ENV=development
