/**
 * Integration Test Setup
 *
 * This file runs before integration tests to set up the test environment.
 */

// Load test environment variables
process.env.NODE_ENV = 'test';

// Add any global test setup here
// For example: database connections, mock servers, etc.

export {};
